#include <stdio.h>
#include <string.h>
int main()
{
    char name[] = "tanvir Anjum";
    // int length = strlen(name); //Can be Typecast.
    printf("Length is:%d", strlen(name));  //But without tyepcast it also can be print.
    return 0;
}
/*Length is:12*/